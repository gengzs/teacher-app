{
  "name": "teacher-app",
  "version": "1.0.0",
  "description": "TeacherApp Web - 教师助手网页端",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "setup": "cd frontend && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && NODE_ENV=production node index.js"
  }
}
